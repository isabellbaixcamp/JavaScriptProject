
function hola() { // conjunto de tareas			alert("buenos dias");
		}

function suma(a,b) {
			return a + b;  // parametros de la funcion		
		}

// sin parametros y con return
function resta() {
	var x = parseInt(prompt(numero));
	var x = parseInt(prompt(numero));
	return x-y;
}

// con parametros y sin return
function multiplicar(a,b) {
	alert("la multiplicacion es " + (a*b));
}