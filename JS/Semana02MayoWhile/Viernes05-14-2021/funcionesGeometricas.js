//area de un triangulo (b*a)

function area_triangulo(a, b) {
	let area;
	area = a * b;

	return area;

}

// area de un circulo (pi * r  2)

function area_circulo(r) {
	let area;
	area = (3.14 * r * r); 

	return area;
}

function area_cuadrado(q) {
	let area;
	area = (q * q);

		return area;
}

function area_rectangulo(x, y) {
	let area;
	area = rec1 * rec2;

		return area;

}